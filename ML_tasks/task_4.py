import csv

def Model_save_log(name, filename):
    with  open(filename, "r", encoding="UTF-8") as f:
        csv.DictReader(f)


def Load_model_logs(filename):
    with open(filename, )

def save_predictions(anem,filename,data):
    pass